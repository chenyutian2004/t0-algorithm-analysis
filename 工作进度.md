# 工作进度

## 已完成

- 确认 `analysis_record` 是数据库表，不新增 `analysis_record.py`。
- 将 `importer.py` 的 CSV 导入与增量写入职责合并到 `database.py`。
- 将 `models.py` 与 `config.py` 的字段映射、清洗、路径、表结构及分组配置合并到 `schema.py`。
- 将 StockDB 个股行情和市场环境的获取、计算、校验及落库集中到 `market_environment.py`。
- `prepare.py` 改为只读取 `stock_market_data`、`market_environment` 和 `trade_record`，构造 `analysis_record`。
- `main.py` 成为唯一总入口，支持 `all/import/market/prepare/algo/account`；`all` 固定执行 `import → market → prepare → algo`。
- 单账户分析接入 `main.py`，但不进入默认 `all` 流程。
- 当前代码层移除 `importer.py`、`models.py`、`config.py`、`profile.py` 和 `prediction_xgboost.py`，预测层暂不纳入主流程。
- README 已直接列明 `analysis_algo` Benchmark A/B/C/D 的全部环境分组参考字段，并注明 D 的显著性单元会自动补入 `trade_date`。
- 已新增 `.gitignore`、`requirements.txt`、`stockdb/README.md` 和 `data/README.md`，明确代码依赖、SDK 接入、数据契约及 GitHub 排除边界。
- 已依据 `hello245m/free-stockdb` 上游仓库重写 StockDB 接入说明，补充 Release/源码安装、数据同步、服务启动、SDK 验证、项目调用和两套 data 目录边界。
- 已详细扩写 Desktop Git/GitHub 操作说明第 8、9 部分，覆盖个人分支工作流、PR、fetch/pull、rebase/merge、冲突处理和安全退出。
- 已将 Git/GitHub 操作说明第 8～11 部分整理为仓库内 `Git协作流程.md`，并在根 README 增加团队协作文档入口。

## 进行中

- 使用新增交易 CSV 复验 `market_environment → prepare` 的完整实际更新流程。
- 评估算法共同环境覆盖率及结果稳定性。

## 待进行

- 扩充交易日后复验 Benchmark 与固定股票高覆盖组的显著性稳定性。
- 完成稳定差异分析后，再决定预测层的数据定义、训练方法和是否接入主流程。
